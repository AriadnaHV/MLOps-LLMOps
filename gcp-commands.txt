
docker build -t gcr.io/"RESOURCE_NAME"/"IMAGE_NAME" .

gcloud builds submit --tag "REGION"-docker.pkg.dev/"RESOURCE_NAME"/"ARTIFACT_FOLDER_NAME"/"IMAEG_NAME":latest .


gcloud auth configure-docker "REGION"-docker.pkg.dev

gcloud run deploy "IMAGE_NAME" \
  --image "REGION"-docker.pkg.dev/"RESOURCE_NAME"/:ARTIFACT_FOLDER_NAME"/"IMAGE_NAME":latest \
  --platform managed \
  --region "REGION" \
  --allow-unauthenticated
660